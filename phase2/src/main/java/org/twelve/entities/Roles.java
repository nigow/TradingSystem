package org.twelve.entities;

/**
 * Enum for different roles a user can have
 * @author Maryam
 */
public enum Roles {
    ADMIN,
    MOD,
    TRUSTED,
    FROZEN,
    BANNED,
    DEMO,
    VACATION,
    NORMAL
}
